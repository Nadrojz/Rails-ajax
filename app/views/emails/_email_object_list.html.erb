<ul>
  <h2>Liste des emails</h2>
  <div id="object-list">
    <% @emails.each do |email|  %>
      <li id="emailid-<%= email.id%>" class="list-group-item">
        <%= link_to email.object, email_path(email), remote:true%>
      </li>
    <% end %>
  </div>
</ul>
